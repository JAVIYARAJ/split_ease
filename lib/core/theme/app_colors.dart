import 'dart:ui';
import 'package:flutter/material.dart';

// --- Theme Colors (Light Mode) ---
class AppColors {
  // Primary Yellow (Slightly darker for better contrast on white if needed, but keeping brand color)
  static const Color primaryAccent = Color(0xFFFFD428);
  static const Color primaryAccentDark = Color(0xFFE6BE20); // Darker shade for pressed states/text

  static const Color backgroundWhite = Color(0xFFFFFFFF);
  static const Color surfaceWhite = Color(0xFFFAFAFA); // Slightly off-white for inputs
  static const Color textBlack = Color(0xFF1A1A1A);
  static const Color textGrey = Color(0xFF424242);
  static const Color borderGrey = Color(0xFFE0E0E0);
  static const Color errorRed = Color(0xFFFF5252);

}